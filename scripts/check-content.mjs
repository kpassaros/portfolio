import fs from 'node:fs';const p=JSON.parse(fs.readFileSync('data/projects.json'));const e=[];for(const x of p){if(!x.links?.documentation||!fs.existsSync(x.links.documentation))e.push(x.id+': documentação ausente')}const h=fs.readFileSync('index.html','utf8'),c=String(p.length).padStart(2,'0');for(const id of ['project-count','public-project-count'])if(!h.includes(`id="${id}">${c}<`))e.push(id+' inconsistente');if(e.length){console.error(e.join('\n'));process.exit(1)}console.log('check-up OK:',p.length,'projetos');