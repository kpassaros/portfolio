# Correção de background — final6-bgfix

Removidas apenas as regras adicionadas na final6 que alteravam opacidade, saturação e gradientes das páginas internas. As páginas principais voltaram ao modelo original compartilhado: canvas neural persistente, camada atmosférica e identidade visual uniforme. A Home, o loading, a navbar e o case CashFlow foram preservados.
